# Claude Wrapped — proxied-mode (pure HTTP, no docker.sock required).
# Start the upstream first: node examples/claude-wrapped/server.mjs
# Then point Floom at this file via FLOOM_APPS_CONFIG.

apps:
  - slug: claude-wrapped
    type: proxied
    openapi_spec_url: http://localhost:4111/openapi.json
    display_name: Claude Wrapped
    description: "Spotify Wrapped for Claude Code. Visualize your AI coding stats from session transcripts."
    category: productivity
