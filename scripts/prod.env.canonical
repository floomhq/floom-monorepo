# Canonical env for prod (floom.dev) — floom-prod-waitlist on :3055.
# Secrets are pulled from the live container by redeploy-prod.sh; this file
# only declares NON-secret keys + secret placeholders.
#
# Why this file exists: prod previously drifted because each ad-hoc
# `docker run` set env piecemeal. After 2026-04-29 OOM event we standardized
# on the same canonical-env pattern as mvp (scripts/mvp.env.canonical).

PUBLIC_URL=https://floom.dev
BETTER_AUTH_URL=https://floom.dev
NODE_ENV=production
DATA_DIR=/data
PORT=3000
# Production is intentionally waitlist-gated: visitors join the waitlist
# instead of creating accounts directly. To open public signup, flip this
# to DEPLOY_ENABLED=true in a reviewed deploy and keep the waitlist endpoint
# available for explicit /waitlist traffic.
DEPLOY_ENABLED=false
# Email verification required (Better Auth gates run/publish behind verified
# inbox). Default is true unless FLOOM_REQUIRE_EMAIL_VERIFY=false.
FLOOM_REQUIRE_EMAIL_VERIFY=true

# Secrets (placeholders — passthrough from live container)
OPENAI_API_KEY=${OPENAI_API_KEY}
GEMINI_API_KEY=${GEMINI_API_KEY}
RESEND_API_KEY=${RESEND_API_KEY}
WAITLIST_IP_HASH_SECRET=${WAITLIST_IP_HASH_SECRET}
# --- email sender (NON-SECRET) ---
# DO NOT REVERT to noreply@. Federico 2026-04-29: Yahoo + Outlook spam-foldered
# every email when From: noreply@. Switched to team@ for ~10-15% deliverability
# lift (industry data on bulk-mail filters' weight on the noreply pattern).
RESEND_FROM=Floom <team@send.floom.dev>
BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
# GitHub OAuth — Federico's app "Floom".
# Callback URL configured: https://floom.dev/auth/callback/github
GITHUB_OAUTH_CLIENT_ID=${GITHUB_OAUTH_CLIENT_ID}
GITHUB_OAUTH_CLIENT_SECRET=${GITHUB_OAUTH_CLIENT_SECRET}
FEEDBACK_GITHUB_TOKEN=${FEEDBACK_GITHUB_TOKEN}
FEEDBACK_GITHUB_REPO=floomhq/floom

# Models + runner
PARSER_MODEL=gpt-4o-mini
EMBED_MODEL=text-embedding-3-small
RUNNER_TIMEOUT=300000
RUNNER_MEMORY=512m
RUNNER_CPUS=1
RUNNER_NETWORK=bridge
BUILD_TIMEOUT=600000

# --- error reporting (server) — Sentry DSN is public-by-design ---
SENTRY_SERVER_DSN=https://0c640f4db88a428519f9868f8ff5f124@o4511300722229248.ingest.us.sentry.io/4511300733108224
SENTRY_DSN=https://0c640f4db88a428519f9868f8ff5f124@o4511300722229248.ingest.us.sentry.io/4511300733108224
SENTRY_ENVIRONMENT=production

# --- analytics — PostHog US cloud, project ID 395246 (write-only key) ---
POSTHOG_PROJECT_API_KEY=${POSTHOG_PROJECT_API_KEY}
POSTHOG_HOST=https://us.i.posthog.com

# Floom flags
FLOOM_CLOUD_MODE=1
# Launch hardening: Docker-image publishing and bundled Docker seed apps stay
# disabled until the Docker runtime path has a separate isolation pass.
FLOOM_ENABLE_DOCKER_PUBLISH=false
FLOOM_SEED_APPS=false
FLOOM_SEED_LAUNCH_DEMOS=false
FLOOM_APPS_CONFIG=/apps/catalog/apps.yaml
FLOOM_FILE_INPUTS_DIR=/floom-file-inputs
FLOOM_FILE_INPUTS_HOST_DIR=/opt/floom-preview-file-inputs
FLOOM_TRUSTED_PROXY_CIDRS=172.16.0.0/12

# Launch-week sidecars enabled on prod for the launch demo experience
# (hook-stats, claude-wrapped, session-recall, floom-this, launch-scorecards).
# Federico 2026-04-29 flip: prod = same demo set as mvp.
FLOOM_LAUNCH_WEEK_APPS=true

# Abuse fuse threshold (R39 load test 2026-04-29): default of 10 trips
# is too aggressive for HN day. One bad actor at 50/min × 5min = 250 trips
# would NOT trigger fuse at 50; old default fired at 10. Buy us headroom
# without disabling the fuse entirely.
FLOOM_ABUSE_FUSE_429_THRESHOLD=50
