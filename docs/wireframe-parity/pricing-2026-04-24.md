# /pricing wireframe parity audit — 2026-04-24

**Outcome: 0 code changes. Current `/pricing` is better than the wireframe on every axis where they differ.**

- **Wireframe:** https://wireframes.floom.dev/v17/pricing.html (v17 revised 2026-04-22)
- **Current page:** `apps/web/src/pages/PricingPage.tsx`
- **Preview verified:** https://preview.floom.dev/pricing (1400x900, screenshot captured 2026-04-24)

## Why this audit exists

Federico 2026-04-24: *"some of them, like pricing are actually better now than wireframes, so dont follow blindly as this will lead to regressions."* — direct guidance that wireframes are references, not specs, and that parity agents must default to KEEP and justify every MATCH against recent PR history.

Per `feedback_parity_not_blind.md`: when fewer than 3 MATCH items surface, the page is fine and the correct artifact is a documentation-only PR proving the audit was done. This is that artifact.

## Recent PR history on `PricingPage.tsx`

| PR | Change | Why it matters for parity |
|----|--------|---------------------------|
| #709 | H1 shortened to "Free. Your own key → unlimited." | Wireframe H1 is "Free. Rate-limited, not paywalled." — current is shorter and more positive. Do not regress. |
| #701 | MIT-license copy removed, canonical docker cmd, CTA gating on `deployEnabled` | Wireframe still has robotic "MIT-licensed." — do not re-add. |
| #696 | Copy polish ("Free to start" era), post-deploy batch | Baseline polish. |
| #676 | Regression sweep — display font restored, pricing copy scope | Display font is now `var(--font-display)` (Inter 800), not DM Serif Display. |
| #665 | Canonical self-host command, legal banner | Docker image path `ghcr.io/floomhq/floom-monorepo:latest` is the real published image. |
| #651 | /apps polish (adjacent) | N/A |

## KEEP / MATCH / SKIP table

Current page is KEEP for every section. Reasons below explain why the divergence from the wireframe is intentional.

| Section | Wireframe says | Current says | Decision |
|---|---|---|---|
| **H1** | "Free. Rate-limited, not paywalled." | "Free. Your own key → unlimited." | **KEEP** — #709 shortened this deliberately. Current frames the upgrade path as positive ("your own key → unlimited") instead of the defensive "not paywalled" negation. |
| **Eyebrow** | "Launch week · 27 April 2026" | Same | KEEP (already matches) |
| **Lede** | "Every app on Floom runs for free on our Gemini key. When you hit the limit, paste your own key for unlimited. Paid tiers come after launch." | "Every app on Floom runs on our Gemini key, no credit card. Paid tiers come after launch." | **KEEP** — tightened after H1 already carries "your own key → unlimited". Avoiding redundancy. |
| **$0 card price** | `$0` / "for everyone, until paid tiers land" | Same | KEEP (match) |
| **Card intro line** | "Run any of the **22** live apps. Publish your own. No credit card, no trial timer." | "Run any of the live apps. Publish your own. No credit card, no trial timer." | **KEEP** — hardcoded "22" count in wireframe would go stale; current is future-proof. |
| **3 limit cells** | 3-column grid of nested cards inside the $0 card | Vertical stacked list with borders | **KEEP** — rewritten 2026-04-23 explicitly because Federico said "this looks messy" (see file header comment). Nested-card-in-card was visual bloat. |
| **"MIT-licensed" copy** | `One Docker command. MIT-licensed. Unlimited concurrency.` (limit cell) and `The core runtime is MIT-licensed.` (self-host strip) | "Open source" in both places | **KEEP** — per MEMORY.md: *"Never write 'Open source. MIT licensed.' or similar robotic license declarations in human-facing copy."* #701 removed this verbatim. |
| **Spec strip** | 5-column grid of cards (Memory / CPU / Run timeout / Max input / Max upload) | Single inline mono line: `512 MB RAM · 1 vCPU · 5 min timeout · 10 MB max input/upload` | **KEEP** — part of the 2026-04-23 "messy" cleanup. 5-column card grid competed with the limit-rows above it for attention. Inline mono line carries the same data with 90% less chrome. |
| **Primary CTA** | `<a>Create your account</a>` (always) | `<Link>` gated on `deployEnabled`: "Create your account" when publishing is live, "Join the waitlist" in waitlist mode | **KEEP** — #701 added `deployEnabled` gating so the pricing CTA can't bait-and-switch users during waitlist mode. Wireframe predates the waitlist-mode runtime flag. |
| **Secondary CTA** | `<a class="btn btn-secondary">Read the docs</a>` (full button) | Text link "Read the docs →" with underline | **KEEP** — one primary button + one secondary text link reads with cleaner hierarchy than two side-by-side buttons, per the 2026-04-23 cleanup note in the file header. |
| **Fine print** | "no credit card · no trial · no paywall · runs logged in /me" | "no credit card · no trial · no paywall" | **KEEP** — dropped trailing "runs logged in /me" clause, which was implementation detail not a user-facing benefit. |
| **Self-host strip docker cmd** | `docker run -p 3000:3000 floomhq/floom-docker` | `docker run -p 3000:3000 ghcr.io/floomhq/floom-monorepo:latest` | **KEEP** — `ghcr.io/floomhq/floom-monorepo:latest` is the actually-published image (see `/opt/floom-docker/`). `floomhq/floom-docker` does not exist on Docker Hub. #665 + #701 canonicalized this. |
| **Self-host guide link** | `./docs.html#self-host` | `https://github.com/floomhq/floom` | **KEEP** — `/docs` does not currently expose a `#self-host` anchor; the GitHub README is the actual self-host reference. Safer than a link to a section that isn't rendered. |
| **Later-plans line** | "Paid plans coming post-launch. Free forever for self-host." | Same | KEEP (match) |
| **FAQ q1** (rate-limit hit) | Same | Same | KEEP (match) |
| **FAQ q2** (self-host) | `docker run floomhq/floom-docker` + "MIT-licensed" | canonical `ghcr.io/floomhq/floom-monorepo:latest` + "open source" | **KEEP** — same two reasons as above: real image path + robotic-copy avoidance. |
| **FAQ q3** (why no paid) | Same | Same | KEEP (match) |
| **FAQ q4** (run timeout + `RUNNER_TIMEOUT`) | Same | Same | KEEP (match) |
| **FAQ q5** (revenue cut) | Same | Same | KEEP (match) |
| **FAQ q6** (ownership) | Single static answer | Conditional: extra sentence about waitlist-mode publishing gate when `deployEnabled === false` | **KEEP** — #701 added the waitlist-mode branch so the answer stays truthful during waitlist mode. |
| **Mobile responsive polish** | Not present (wireframe is desktop-only 1440px frame) | `<640px` overrides scaling hero 52→34px, $0 72→56px | **KEEP** — current has an a11y improvement the wireframe never captured. |

## Federico preferences preserved (verified against code)

All five items from the mission brief hold on current `main`:

- [x] H1 is `Free. Your own key → unlimited.` (PricingPage.tsx L330-332)
- [x] Primary CTA gated on `deployEnabled` and reads `Join the waitlist` in waitlist mode (L509-527)
- [x] No redundant "Publish waitlist" secondary link (only `Read the docs →`)
- [x] No "MIT-licensed" robotic copy anywhere (replaced with "Open source" at L88 and L601)
- [x] Canonical docker cmd `docker run -p 3000:3000 ghcr.io/floomhq/floom-monorepo:latest` (L133, L624)

## Conclusion

Zero code changes. The /pricing page has been deliberately polished beyond the v17 wireframe across six PRs (#665, #676, #696, #701, #709, and the 2026-04-23 cleanup). Every delta is intentional and preserves a user-visible improvement. Matching the wireframe would regress five Federico-approved decisions.

This file is the audit artifact. Future parity agents touching `PricingPage.tsx` should read it before proposing wireframe-alignment edits.
