# Non-repo Floom-titled assets (inventory 2026-04-21)

Marketing wireframes, screenshots, videos, and scratch exports **do not belong in the application monorepo** unless explicitly imported (often with Git LFS). This file records where they were consolidated on disk so nothing “only on Desktop” is lost without a paper trail.

## Canonical code checkout

- **Repository:** `floomhq/floom`
- **Path:** `~/floomhq/floom` (only Git remote `https://github.com/floomhq/floom.git` for app code)

## Bulk archive folder (this session)

To keep the Mac tidy, Floom-titled **non-git** material was moved to:

`~/Archive/legacy-floom-assets-20260421/`

Layout:

| Subfolder | Former location |
|-----------|-----------------|
| `desktop/` | `~/Desktop/floom*` files and directories |
| `documents-misc/` | `~/Documents/misc/floom-launch-video`, `~/Documents/misc/wireframes-floom` |
| `documents-scratch/` | `~/Documents/floom-scratch` |

Update this table if you move or delete the archive.

## Still outside `floomhq/floom` (intentionally)

- **Context vault notes** (`~/context/.../floom`) — not product code; stays in the `context` repo.
- **Browser download debris** (`~/Downloads/floom*_*files`) — safe to delete when not needed.
- **OpenSlides example** (`~/openslides/examples/floom-demo`) — tutorial asset.
- **`~/Archive/floom-home-clone-20260421.bundle`** — old full-object git bundle; keep until stash recovery is verified, then optional delete.
